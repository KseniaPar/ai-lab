# Cursor User Rules — Knowbase companion (paste into Global User Rules if empty)

When working in repo `ai-lab` / Knowbase:
- Prefer project `.cursor/rules` and `.cursor/skills` over generic advice
- Reply in the user's language
- Never commit `application-local.yml` or API keys
- Do not invent concept-map features

(This file documents the intended global layer for Day 1; copy into Cursor Settings → Rules → User Rules.)
